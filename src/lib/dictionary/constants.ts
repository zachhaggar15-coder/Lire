/** Shown in the live word popup when the local dictionary has no entry for a word. */
export const NO_DICTIONARY_ENTRY = "Not in local dictionary yet";

/** Stored as a SavedWord's primaryTranslation when it was saved with no dictionary entry. */
export const NOT_TRANSLATED_YET = "Not translated yet";

/** Fallback learner example sentence used when neither the dictionary nor AI has one. */
export const FALLBACK_EXAMPLE_FR = "Je vois ce mot dans un texte.";
export const FALLBACK_EXAMPLE_EN = "I see this word in a text.";

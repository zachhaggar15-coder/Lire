/node_modules
/.next
/.next-verify
/out
/build
.DS_Store
*.pem
*.jks
*.keystore
android-keystore
android/local.properties
android/.gradle/
android/app/build/
android/build/
android/*.aab
android/*.apk
npm-debug.log*
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Downloaded dictionary source data (scripts/build-dictionary.mjs re-downloads
# it if missing) — only the generated JSON output is committed.
/.cache

.vercel
.env*

# Claude Code runtime state (session locks, local-only settings) — .claude/launch.json is the only intentionally-tracked file in this folder.
.claude/*.lock
.claude/settings.local.json

# Local dev-server logs (e.g. port-collision output from `next dev`).
.next-dev-*.log
dev-server-*.log
codex-next-dev*.log

# Bubblewrap signing metadata (build artefact)
android/*.idsig

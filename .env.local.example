# Copy this file to .env.local (which is gitignored) and fill in your key.
# Never commit real API keys.

# Which AI provider to use. Only "openai" is implemented today; see
# src/lib/ai/providers/index.ts for how to add another.
AI_PROVIDER=openai

# Required when AI_PROVIDER=openai. Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional — defaults to gpt-4o-mini (cheap, fast, good enough for this app's
# short texts). Any OpenAI chat-completions model that supports JSON mode works.
OPENAI_MODEL=gpt-4o-mini
